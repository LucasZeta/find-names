<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Find Names - Results for {{ query }}</title>
  </head>
  <body>
    <h1>Results for {{ query }}</h1>

    <ul>
      {{#each results}}
        <li>There are {{ this.count }} occurrencies of the name {{ this.name }}</li>
      {{else}}
        <li>There are no occurrencies for the searched term.</li>
      {{/each}}
    </ul>

    <p>Type any combination of letters</p>
    <input type="text" id="name" name="name" />
    <input type="submit" value="Search" onclick="redirect()" />

    <script type="text/javascript">
      function redirect() {
        window.location.href = './' + document.getElementById('name').value;
      }
    </script>
  </body>
</html>